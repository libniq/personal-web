import React from 'react';

const subsectionTitle = (props) => (
    <div className="row" style={{marginTop:'24px'}} >
        <div className="col-md-12">
            <h3 style={{fontSize:'40px'}} >{props.title}</h3>
        </div>
    </div>
);

export default subsectionTitle;




